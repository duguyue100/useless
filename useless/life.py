def is_livable(city: str) -> bool:
    """Determines if a city is livable based on its name.

    Hard truth: all cities are livable.

    Args:
        city: The name of the city to check.

    Returns:
        bool: True if the city is livable, False otherwise.
    """
    return True
